const path = require('path')

const IMAGES_DESTINATIONS = {
          pv: path.sep + 'uploads'+path.sep+'pv',
          dossiers: path.sep + 'uploads'+path.sep+'dossiers',
}

module.exports = IMAGES_DESTINATIONS